# pyTaskFlow

PySide6 で作られた、JSON ベースのタスク依存関係ビューア / エディタです。
タスク、成果物、依存関係を編集しながら、依存グラフをドラッグ操作や自動配置で整理できます。

## 主な機能

- タスク依存関係を `QGraphicsView` 上にノードとエッジで表示
- タスク、成果物、依存関係を左ペインから追加 / 編集 / 削除
- JSON プレビューを見ながら編集
- ノードのドラッグ配置と座標の JSON 保存
- 依存関係に基づく完全自動配置
- 既存の縦位置を維持した列だけ整列
- クリック操作による依存関係の追加
- タスクのダブルクリックによる入出力 / 参照資料の展開表示
- タスク、成果物、資料リンクを右クリックメニューから開く
- 保存時の簡易バリデーション

## 必要環境

- Python 3.10 以上
- PySide6 6.5 以上

## ライセンス

pyTaskFlow 本体は [MIT License](LICENSE) で提供されます。

このアプリケーションは Qt for Python の `PySide6` を使用しています。PySide6 / Qt は、オープンソース利用では LGPLv3、GPLv2、GPLv3、または商用ライセンスで提供されています。このプロジェクトでは PySide6 を LGPLv3 として利用する想定です。

第三者ライブラリとそのライセンスは [THIRD_PARTY_LICENSES.md](THIRD_PARTY_LICENSES.md) を参照してください。LGPLv3 および関連する GPLv3 の全文は `licenses/` に同梱しています。

PySide6 / Qt を同梱して配布する場合は、Qt を使用していることの明示、LGPLv3 の全文提供、利用者が LGPLv3 に基づく権利を行使できる形での配布条件を確認してください。

## インストール

```bash
pip install .
```

開発用に編集可能インストールする場合:

```bash
pip install -e .
```

依存パッケージだけ入れて直接起動する場合:

```bash
pip install PySide6
```

## 起動

インストール後:

```bash
pytaskflow
```

モジュールとして起動:

```bash
python -m pytaskflow
```

リポジトリ直下から直接起動:

```bash
python main.py
```

## 基本操作

- `新規`: 空のプロジェクトを作成します。
- `JSONを開く`: 既存のプロジェクト JSON を読み込みます。
- `保存`: 現在の JSON ファイルへ保存します。
- `名前を付けて保存`: 保存先を選んで JSON を保存します。
- `完全自動配置`: 依存関係の深さから列を決め、行順と縦位置も自動調整します。
- `列だけ整列`: 依存関係の深さから X 座標だけを整列し、Y 座標は維持します。
- `依存追加`: 依存元タスク、依存先タスクの順にクリックして依存関係を追加します。
- `全体表示`: グラフ全体が見えるようにズームを調整します。
- `Ctrl + マウスホイール`: ズームイン / ズームアウトします。
- タスクをドラッグ: ノード位置を手動調整します。
- タスクをクリック: 左ペインの編集対象にします。
- タスクをダブルクリック: 入力、出力、参照資料を展開 / 折りたたみします。
- タスクを右クリック: タスクや関連成果物のリンクを開く、ステータスを変更するメニューを表示します。
- 空白部分をクリック: 選択や依存追加中の始点を解除します。

## 編集できるデータ

左側の `Project` ペインには次のタブがあります。

- `Tasks`: タスクを編集します。
- `Artifacts`: 成果物、マニュアル、参考資料、テンプレートなどを編集します。
- `Dependencies`: タスク間の依存関係を編集します。

`Editor` ペインでは、選択中の項目に応じてフォームが切り替わります。
`JSON` タブでは、現在のプロジェクトデータを整形済み JSON として確認できます。

## JSON 形式

プロジェクト JSON は次のような構造です。

```json
{
  "project_name": "Sample Project",
  "tasks": [
    {
      "id": "T01",
      "name": "Requirements",
      "status": "未着手",
      "group": "Planning",
      "inputs": [],
      "outputs": ["A01"],
      "resources": ["M01"],
      "x": 0,
      "y": 0
    }
  ],
  "artifacts": [
    {
      "id": "A01",
      "name": "Requirements Document",
      "link": "C:\\Project\\requirements.xlsx"
    }
  ],
  "dependencies": [
    {
      "from": "T01",
      "to": "T02"
    }
  ]
}
```

### Tasks

- `id`: タスク ID。依存関係から参照されます。
- `name`: タスク名。
- `status`: ステータス。色分け表示に使われます。
- `group`: グループ名。グループごとに背景色付きの帯で表示されます。
- `inputs`: 入力成果物 ID の配列。
- `outputs`: 出力成果物 ID の配列。
- `resources`: 参照資料 ID の配列。
- `x`, `y`: グラフ上の表示座標。

### Artifacts

- `id`: 成果物 ID。タスクの `inputs`、`outputs`、`resources` から参照されます。
- `name`: 表示名。
- `link`: URL、ファイル、フォルダへのリンク。

### Dependencies

- `from`: 依存元タスク ID。
- `to`: 依存先タスク ID。

## 自動配置

列番号は依存関係の深さから決まります。

```text
column(task) = 0                              # 前提タスクがない場合
column(task) = max(column(predecessor)) + 1   # 前提タスクがある場合
```

完全自動配置では、同じ列の並び順を調整してエッジ交差を減らし、後続タスクを前提タスク群の中心に近づけます。
循環依存がある場合は自動配置できません。

## 保存時のチェック

保存時には次の内容を検証します。

- タスク ID と成果物 ID が空でないこと
- タスク ID と成果物 ID が重複していないこと
- 依存関係が存在するタスク ID を参照していること
- 依存関係が重複していないこと
- 依存関係が循環していないこと
- タスクの `inputs`、`outputs`、`resources` が存在する成果物 ID を参照していること
